{
  "name": "markengine",
  "version": "0.1.0",
  "description": "Standalone markdown parser and renderers (HTML, React, terminal)",
  "license": "MIT",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "oxlint .",
    "build:engine": "tsc -p tsconfig.json --outDir dist --module commonjs --declaration false --target ES2022",
    "dev:engine": "tsc -p tsconfig.json --outDir dist --module commonjs --declaration false --target ES2022 --watch --preserveWatchOutput",
    "dev": "concurrently -k -n ENGINE,VITE -c cyan,green \"npm:dev:engine\" \"vite --host --port 3000 --strictPort\""
  },
  "dependencies": {
    "react": "^19.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "@types/react": "^19.1.2",
    "oxlint": "^1.24.0",
    "concurrently": "^9.2.1",
    "typescript": "^5.9.3",
    "vite": "^7.1.8"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@10.7.1"
}
